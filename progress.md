# Progress

## 2026-09-20
- Read brainstorming and planning-with-files skills.
- Inspected workspace, tool availability and Windows network configuration.
- Classified task as architectural; preparing capability-aware design.
- No implementation or router settings changed.
- Read-only gateway inspection identified an ARRIS interface and limited public status; no authenticated session established.
- Saved a reviewed architecture proposal with platform alternatives, monitoring sources, profile identity, capability limits and implementation acceptance checks.
- Pending user decision on strict zero-input access versus one-time router pairing. No SDK installation or implementation started.
- User instructed 'weiter'. Proceeding with no-login monitoring by default and optional app-owned router pairing; not requesting credentials in chat.
- Created implementation plan; proceeding inline.
- Installed local .NET SDK 10.0.401. Initial restore/workload failed because inherited NuGet configuration had no sources; adding project-scoped official NuGet source.
- Installed maui-windows workload, scaffolded MAUI host, implemented Core/Windows libraries and nine German dashboard views.
- Upgraded SQLite bundle to 3.0.5 and Microsoft.Data.Sqlite to 10.0.12 after package vulnerability finding. Builds have no warnings.
- 14 core/persistence checks pass; live collector verified actual WLAN, gateway, public ARRIS firmware/DOCSIS, neighbors, process connections and Windows events.
- Native debug app launched PID 34872; task-only CDP 9237 enabled via process environment for UI QA. Screenshot artifacts/overview.png shows real populated data.
- Implemented optional OS-secret-store credentials, per-profile isolated router WebView2, navigation restriction, credential autofill without automatic submit, network-change window closure.
- Implemented DNS diagnostic, profile editor/history, JSON export, retention/interval settings and optional bounded UDP syslog receiver.
- Read requesting-code-review skill, delegated independent read-only review while testing native UI.
- Added the approved device timeline/calendar design and implementation plan; implementation now includes versioned timeline storage, interval aggregation, iCalendar export, CalDAV configuration/client queue, adaptive calendar UI and dark-mode/readability styles.
- Release tests and Windows publish pass after the calendar work; no authenticated router writes or real credential entry performed.

## 2026-09-24
- User asked for the app 'als apk'. Reviewed the workspace: app was Windows-only (`net10.0-windows`), `SysWlan.Windows` referenced unconditionally, router window and export bound to `Microsoft.UI.Xaml` and WebView2. Asked four decisions; user chose full parity, device-wide traffic via NetworkStatsManager, optional foreground service, and cleartext HTTP for the LAN router UI.
- Installed the `maui-android` workload into `.tools`. Found existing Android SDK (`C:\Users\HP\Android`, platforms 34/36, licenses accepted) and JDK 21, so no 2–4 GB download was needed; no device or emulator image available.
- Core: added `AndroidWifiMapping` (frequency→band/channel, RSSI→percent estimate, WiFi standard and security mapping, explicit permission warnings), `GatewayIdentityPolicy` (gateway MAC vs accesspoint BSSID, since Android exposes no gateway MAC and the session policy requires a hardware identity), `NetworkSnapshot.GatewayMacSource` and `AppReceivedBytes/AppSentBytes`, `RouterSyslogDeviceParser` plus `RouterSyslogDeviceFeed` (the only device source under Android), `MonitorService.SetBackgrounded`/`BackgroundIntervalSeconds`, and a `Received` event on `SyslogReceiver`.
- New `src/SysWlan.Android` adapter: AndroidCollector (ConnectivityManager/NetworkCapabilities/LinkProperties, guarded WifiInfo reads), network event recorder replacing the Windows event log, device-wide traffic via NetworkStatsManager with an explicit usage-access requirement, and permission/location-switch checks.
- `SysWlan.App` now multi-targets Windows and Android. Platform actions moved behind `IPlatformActions`, `IPlatformInfo`, `IPlatformPermissions`, `IBackgroundMonitoring`; Windows implementation moved to `Platforms/Windows`, Android implementation added under `Platforms/Android` (router page with origin/navigation/download/SSL rules and cookie purge, export via share sheet, foreground `dataSync` service with notification and wakelock, lifecycle pause).
- Manifest: network/wifi permissions, `NEARBY_WIFI_DEVICES` with `neverForLocation`, location capped at API 32, notification/foreground/wakelock, `PACKAGE_USAGE_STATS` only via system settings, `allowBackup=false`, network security config permitting cleartext for the LAN router, notification drawable. Min API 26, target API 36.
- Views and CSS: platform-neutral labels and capability matrix, permission/background section in settings, background-gap notice, new `mobile.css` with a horizontal tab strip and 44-px tap targets for phones.
- Build: `scripts/build-android.ps1` (workload/SDK/JDK bootstrap, tests, debug and release APK, keystore outside the repo, `apksigner` hint). Windows publish had to move to `-f` plus `RuntimeIdentifierOverride=win-x64`, because a global RID broke restore for the Android TFM and its library. Android release disables trimming and AOT on purpose, so the APK is large.
- Verification: 36 core checks pass (8 new Android/identity/syslog/background checks), Windows publish plus a live Windows smoke test (window title present, fresh SQLite database with monitoring and network log entries), Android debug and release builds, merged manifest inspected with `aapt dump permissions`, release APK signature checked with `apksigner` (debug key: test only), package contents checked (wwwroot assets and notification drawable included). No device test possible.

## 2026-09-25
- User asked to test the Android APK on a connected device. A Xiaomi POCO `zorn_global` (Android 16/API 36, arm64-v8a, 480 CSS px wide) was attached; the phone was running its own hotspot, so its WLAN client was disconnected.
- HyperOS blocked `adb install` until "USB debugging (Security settings)" was enabled and kept `adb shell input` blocked. The UI was driven through the WebView DevTools protocol (`adb forward` to `@webview_devtools_remote_<pid>` plus a standard-library CDP client under `artifacts/`), with `uiautomator dump` for system dialogs and `run-as` for database and export files.
- Device run: all ten views render; address, gateway and DNS match `dumpsys connectivity`; missing WLAN values stay "—" with a reason; the permission buttons grant `NEARBY_WIFI_DEVICES` and open the usage-access system page; device-wide traffic matched a 400 MB download through the hotspot (+460 MB shown, 7 % off the drift-adjusted expectation); the foreground `dataSync` service runs with an ongoing notification and keeps collecting in the background (124 s without a gap), while without the service the history shows exactly one 78 s gap; JSON and `.ics` exports reach the share sheet without credentials; synthetic router syslog produced two devices and no device for an all-zero MAC; history and profiles survived update, restart and process end; portrait and landscape render without page overflow.
- Fixed five findings the device exposed: working debug APK (`EmbedAssembliesIntoApk=true`, the old APK aborted with `No assemblies found`), `LocationDisabledWarning` now reads the real location switch instead of inferring it from a missing SSID, the platform-free start log says "dieses Geräts" instead of "dieses PCs", `overflow-wrap:anywhere` for paragraphs (the long CalDAV path had pushed Android's layout viewport from 480 to 543 px), and the Android export writes without a UTF-8 BOM like Windows. Tests extended for the location-switch case; build, reinstall and re-verification on the device are done.
- Still open: real WLAN client values and the router sign-in in the WebView need the phone connected to a real WLAN with a router; CalDAV sync and the Android 15 six-hour `dataSync` limit need a longer run.
